"""Web-layer services for apply-flow orchestration.

Currently exposes only the apply-session IPC primitives (Phase 3.3) and the
apply-run event log (Phase 3.4). Other apply-flow helpers still live in
``cli.py`` pending the rest of the Phase 2.1 refactor.
"""
